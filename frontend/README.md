# DeciMeet Web

DeciMeet 的 React + TypeScript 前端，包含会议、纪要、决策档案、RAG 对话、知识库和 AgentRun 工作台。

```bash
npm ci
npm run dev
```

开发服务器默认监听 <http://localhost:5173>，并将 `/api` 代理到 <http://localhost:8787>。

```bash
npm run lint
npm run build
```

完整架构、演示步骤和项目边界见仓库根目录的 `README.md`。
